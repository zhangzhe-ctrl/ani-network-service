# NET-05A 方案交付与静态检查

日期：2026-09-10。执行位置：本地 `/home/chabking/workspace/ani-network-service`。范围：方案、规格、ADR、文档导航与编排；未执行业务代码、迁移、编译、集群或远程测试。

## 用户决定与输入

用户要求在 NET-06 前增加持续观察方案，并将配额接入推迟到 Core 重构之后。按此新增独立 NET-05A，保留 NET-05 和 NET-06 身份；实施顺序和未来配额安排以[总计划](../../plans/vpc-subnet.md)为准。当前工作包状态只在[执行状态](../status.md)维护。

读取时 Git HEAD 为 `5d4a53451beca0317bee1f577d8fb9cc189fc035`。工作区已有 START-HERE、执行状态、选型研究、KC-KIND 及 NET-02–04 发布记录改动；本任务按现有内容增量修改相关文档，没有覆盖其他任务的运行记录、源码或发布决定。该 HEAD 是设计输入，不是未来 NET-05A 的自动实施基线；后续应固定 NET-05 已验收版本。

规则来源：主规格、ADR-0001～0003、当前总计划、执行状态，以及[选型主源记录](2026-09-10-cr-observation-selection.md)。选型时源文件指纹是历史快照，新增规范已修改主规格，不能继续声称当前全部文件与旧指纹一致。

## 交付

- [ADR-0004](../../adr/0004-observe-cr-with-durable-reconciliation.md)：观察与持久执行的取舍、框架自主选择、配额延期。
- [持续观察规格](../../specs/cr-observation.md)：目标行为与 V-16～19；主规格引用增量，避免复制调度规范。
- [NET-05A 方案](../../plans/cr-observation.md)：六步实施、输入、计划路径、升级/恢复及新版本验收。
- 总计划新增 NET-05A、NET-06 前置依赖；当前状态保持实施 `not_started`、运行 `not_verified`；导航和原研究记录指向正式材料。

## 静态检查

实际执行 `git rev-parse HEAD`、`git status --short`、`git diff --stat`、`git diff --check`；本地 `python3` here-doc 读取方案相关 Markdown，检查本地链接目标、标题锚点、围栏配对、行尾空白，并断言 NET-05/05A/06 顺序、NET-05A 未启动、主规格 V-01～19 唯一且连续。检查结果属于文档结构，不是运行行为。

| 检查 | 结果与边界 |
|---|---|
| 本地链接、锚点、围栏、空白及增量 diff | `pass`，本地静态检查 |
| 工作包顺序、状态和验收编号 | `pass`，NET-05A 位于 NET-05 与 NET-06 之间；未启动实施；旧验收编号保留 |
| 独立设计复核 | `pass`，已修正跨副本旧缓存误回写/误完成通知，以及增强轮询的比较理由；不代表实现验证 |
| 业务实现、生成/编译、真实 PG、受控 Watch、多副本、容量和真实数据面 | `not_verified`，本轮未执行 |
| 跨仓改造、VM、配额、部署和切流 | 未执行；方案不把它们记为已完成 |

新增要求包括：独立 Watch 存在时 V-09 分开验证查询纯读与应用停顿；公开 version 保持现有并发语义；旧 NET-05 证据不能替代新观察实现验收；配额不预建表、RPC 或 outbox。具体约束只以规格和计划为准。

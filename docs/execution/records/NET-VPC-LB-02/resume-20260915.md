# NET-VPC-LB-02 恢复输入与执行边界

2026-09-15 用户针对旧集群限制补充新环境信息并询问能否恢复，随后确认标签方式与构建记录责任。本记录承接这些新指示；[原 Goal](goal-objective.md)保留原文，当前执行状态仍只看[状态页](../../status.md)。

后续指示：用户在三节点对照后明确要求以 untagged 配置 ani-01 ens35 并继续测试，取代本文最初的主机 tagged 102 要求。实际执行和当前差异见[untagged IP 测试](l2-diagnostic-20260915/untagged-followup.md)，本文保留原恢复时点。

## 新输入

- 恢复目标：172.16.101.10–12 的 ani-platform 集群，context `kubernetes-admin@ani-platform`，kube-system UID `be57b911-892c-4e75-aa9d-4a05d819c59e`。本次明确补充新环境并要求恢复，作为调整原 kind 验收目标的指示；原 kind 和新集群已有手工 LB 场景均保留。
- 用户授权 ens35 接管；VLAN 102、`172.16.102.0/24`、上游网关 `172.16.102.1`。追问后用户明确：“主机需要打 VLAN 102 标签”。因此新实验 VLAN 使用 tagged 102，不能按 untagged/0 配置。该授权用于 U09 所需实验出口准备；不改变租户 IPv4、small 双副本与原 11 项验收要求。
- 构建在原 SSH `ubuntu` 完成，经本机中转到新集群。用户要求：“你自己操作自己记录就行”。构建/导出/中转/导入的 manifest、产物哈希与运行 imageID 由本任务实际采集，不要求用户另提供现成记录，也不将这句话当作已经获得构建证明。
- 原 Goal 其他边界保持：独立 Network 工作树、固定 e534 基线、独立数据库和资源、实际产品 API、业务实例 owner 协议；不修改 kc/Envoy 源码或重启共享控制器，不提交、推送、合并或发布。

## 本次复核事实

- [新集群预检](resume-environment-preflight-20260915.json)全部读取成功；[预算](resume-environment-budget-20260915.json)显示三节点无污点、无 Pending，剩余 CPU 10.99/9.98/11.15 核。
- [三台接口与路由](resume-environment-interfaces-20260915.json)：ens35 均 UP、没有主机 IP、已从属 OVS；管理地址与默认路由位于 ens34。
- [OVS 与平台配置](resume-ovs-provider-20260915.json)：三台均已有 br-ens35；kcn-config 的 managedDevices 已包含 ens35。当前没有 VlanNetwork、EIPGateway、EIP、SNAT；无需为了这一步改写共享 managedDevices。
- [VLAN 102 网关 ARP](resume-vlan102-gateway-20260915.json)：每台从 ens35 发送两次 tagged 102、sender IP 为 0.0.0.0 的 ARP 请求，没有收到对应回复，结果为 `not_verified`。此有限诊断不能单独证明网关不可达或 VLAN 配置错误；仍需正常实验地址下的二层/三层与真实业务流量验证。没有增加地址、路由、VLAN 接口或修改 OVS。
- [Provider 来源关联](provider-provenance-20260915.json)：从实际运行 ELF 提取大小写正确的 kc `COMMIT`/`VERSION`/`BUILDDATE`，得到 `git-735abf8`、v0.6.2 和 2026/09/04-15:44:02；只读 kc 仓库唯一解析为 `735abf81cb728f30bd0f15e1885dcd7f88b56ab0`，与当前参考 HEAD 仅有部署示例和文档差异。早期仅检查标准 Go vcs 字段而遗漏大写链接符号的结果保留，不能继续据此称 kc 来源未知。Envoy Gateway revision 为 `94850d4323922cba7c67b5b5ad23de9c715f8048`；[实际 Envoy/shutdown-manager](provider-lb-buildinfo-20260915.json)分别记录 `0ebfcfe5b0484b89ca85b761da9e05ce75dbda8d/1.38.3/Clean` 和 `4de02efc8f05292e6a577b1a5d2106508d03ed46`。这些证明运行二进制内嵌来源与实际 imageID 的关联，不是可复现构建或数据面通过证明。
- [普通 sender IP 的 tagged ARP](continuation-vlan102-ordinary-arp-20260915.json)、[跨节点接收](continuation-vlan102-cross-capture-20260915.json)和[tagged/untagged 对照](vlan-tag-differential-20260915.json)均未收到网关回应；发送计数增加且无发送错误。ens34 正向控制证明抓包接口可用。未配置主机地址或改变 VLAN/OVS。用户确认虚拟化平台是 ESXi/vSphere；已给出 ens35 对应端口组 VGT、物理 uplink VLAN102 和网关接口的具体检查项，原因仍未定位。
- [live-01 计划](live-01/plan.json)与[bootstrap 回执](live-01/bootstrap-result.json)：已创建并核对 12 个专属 Namespace/ServiceAccount/RBAC 对象。初次 create 成功后本地错误地将多段 JSON 当成单个 JSON 解析；没有重放 create，逐对象 GET 核对 UID、标签、规则和 subjects 后恢复回执。此步骤未创建产品 CR。

## 当时的继续执行安排

本段记录恢复时点的安排；随后用户要求暂记阻塞，停止与保留对象见[2026-09-15 停止记录](blocked-20260915.md)。

安装冻结工具改为只接受上面固定 context 与集群 UID，并验证误用旧 context 或将旧集群挂在新 context 名下都会在读取安装 bundle 前拒绝。产品业务逻辑、Provider 适配、数据库和既有受控门禁源码没有因此变化。

[本轮 ubuntu 门禁与构建](resume-build-gates-20260915.json)已通过 make verify、lb-api race 和 network/lb-api 构建。安装冻结工具已实际走完[ubuntu → 本机 → ani-test-1](resume-transfer-20260915.json)，三端 SHA-256 相同；在新集群执行只读冻结成功，[安装指纹](resume-installation-20260915.json)为 `d632b5e7d5de7c442055cf8200b84d9e22a4e03826b2d1843d78686a6778a5e3`。节点临时工具和 JSON 已在证据回传后移除。这是安装工具的二进制传输和只读执行，尚不是 Network 服务镜像部署或产品流量验收。

继续使用已验证的 ubuntu 独立 run 产物，按需中转并核对相同哈希。固定实际 Provider 来源与运行镜像、独立 Public/Intranet 池和实验地址后，按产品 API 逐项执行 U09。现有手工 LB 响应、预检成功及网卡授权均不替代完整 U09 实测；当前流量矩阵继续保持 `not_verified`。

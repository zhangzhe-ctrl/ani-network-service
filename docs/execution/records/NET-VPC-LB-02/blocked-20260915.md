# 2026-09-15 用户要求暂记环境阻塞

用户在确认 vSphere 已改为 VLAN trunking、随后重测仍无回复后指示：“不行就先阻塞吧”。本任务据此停止继续探测、创建和诊断。当前卡片状态只在[执行状态](../../status.md)维护；本记录保留停止时点与恢复输入。

## 出口事实

[三节点重测](vlan102-after-vsphere-change-20260915.json)均从 ens35 发送 tagged VLAN 102 ARP，未收到 172.16.102.1 回复。[普通 sender IP 对照](vlan102-after-vsphere-ordinary-20260915.json)同样没有回复；[跨节点抓包](vlan102-after-vsphere-peer-20260915.json)中 ani-02 未收到 ani-01 测试广播，ens35 接收计数没有增加。

用户报告了端口组修改；本任务没有 vCenter 管理入口，未独立核对端口组实际值、MAC 对应关系或上联。上述结果不足以定位具体故障点，也不证明网关损坏。没有改宿主 IP、路由、防火墙、OVS 或共享控制器。

恢复出口测试需要 ens35 对应端口组与物理上联可传递 tagged VLAN 102，且网关 172.16.102.1/24 可从该链路回应。具体检查位置见[vSphere 检查说明](vsphere-vlan102-check.md)。用户再次要求恢复前，不自动重测或改配置。

## 本次新增的实际结果

- [Provider 来源](provider-provenance-20260915.json)与[Envoy/shutdown-manager](provider-lb-buildinfo-20260915.json)已关联实际运行二进制和 imageID；不再以缺少用户提供的构建记录阻塞。
- [ubuntu API 连接](live-01/api-relay.json)经本机的专属 SSH loopback 转发，通过 TLS、证书名、固定集群 UID 与安装指纹核对。直接 TCP 成功但 TLS 超时的原结果独立保留。
- [真实进程/API 冒烟](live-01/api-smoke-01.json)：标准 Network 服务、同一真实 PG/worker/adapter、独立 owner 和固定租户/管理员入口已实际运行。LB 安装能力观察为 true；标准入口无授权调用返回 PermissionDenied；租户 VPC 列表为空。这不等于 LB 数据面验收。
- [CreateIntranetAddressPool](live-01/api-intranet-create.json)经真实产品 API 受理。最初完整审计耗时约 5.94 秒、超过 5 秒请求预算；[受限配置对照](live-01/request-budget-diagnostic.json)将请求预算改为 10 秒、lease 改为 40 秒，其他新鲜度和资源上限保持。没有修改 Go 业务代码或直接补产品状态。
- [停止后的数据库核对](live-01/blocked-database-state.json)确认 create operation 已在 19:41:04 UTC 成功，池为 available；[实际 Subnet](live-01/blocked-cluster-inventory.json)具有对应 UID 和当前 Valid/Initialized/Ready。池未填写 verification、未开放分配；VPC 数为 0。只能记为平台池创建及观察通过，U09 的 11 项流量矩阵仍全部 not_verified。

## 停止与保留现场

[停止回执](live-01/blocked-runtime-preservation.json)确认五个测试进程都已结束，最后的 systemd unit 为 inactive/dead；[API 转发](live-01/api-relay-stop.json)已关闭，远端端口不存在。没有后台重测或任务服务继续执行。

保留以下本任务对象用于恢复，未执行产品删除或最终清理：

| 对象 | 固定身份 |
|---|---|
| 运行目录 | `/home/ubuntu/workspace/ani-network-service-runs/lb02-09141908-2b3122/private` |
| PostgreSQL | `net_vpc_lb_02_2b3122`，容器 `lb02-09141908-2b3122-pg`，仍运行，768 MiB / 1 CPU 上限 |
| 私有备份 | 上述目录的 `blocked-20260915.dump`；SHA-256 `2878e9885fa664720d3c84bbdd278db71042dab0b721ecbd8ab220eca0ae7829`；归档目录可读，恢复测试未执行 |
| 内网池 | `pool_f6c078d198ae4ea1be6f43cfad8f9934`，CIDR `10.242.252.0/24`，allocation=false |
| Provider Subnet | `kcn-system/pool-f6c078d198ae4ea1be6f43cfad8f9934`，UID `0ce012b9-3943-420e-8cd1-16cff3656a70` |
| 已完成 create operation | `acc683b6-f1c9-4270-83ff-cb676b3ba700` |
| 基础 fixture | 12 个 namespace/SA/RBAC，逐对象 UID 见[清单](live-01/blocked-cluster-inventory.json) |

数据库位于容器 tmpfs，不能先停止或删除该容器而假定数据仍然存在。备份与私有配置不进入源码归档。最初停止说明基于较早查询曾称 operation 未完成；最终只读核对已经纠正为停止前成功。

## 恢复顺序

用户再次授权继续后，先读取本记录、[live-01 计划](live-01/plan.json)和实际对象 UID。保留同一数据库与资源身份，不重放初始化、namespace/RBAC 创建或池创建。

重新建立本任务专属的 API loopback 转发，验证 TLS、集群 UID 和安装指纹。私有 kubeconfig 使用本任务短期 ServiceAccount TokenRequest；过期时只更新该测试 SA 的短期 token，不复制 admin kubeconfig。先确认记录中的 五个进程 仍已停止、原 PostgreSQL 和备份仍存在，再在共享 `net05a-heavy.lock` 与原资源上限下使用已固定源码脚本恢复服务：

```sh
python3 /home/ubuntu/workspace/ani-network-service-runs/snat-20260914T193831Z-aeb6d141/source/scripts/lb-live-runtime serve \
  --directory /home/ubuntu/workspace/ani-network-service-runs/lb02-09141908-2b3122/private
```

该命令须通过与[runtime-start-03](live-01/runtime-start-03.json)等价的受限 systemd unit 执行，不能裸启动绕过锁和资源上限。先 GET 已有池及 operation，等待观察恢复，再继续实际验证与分配流程。最终清理先经产品 API 删除池并核对 Provider 释放，再删除数据库及基础 fixture；不以删 namespace 或数据库掩盖生命周期问题。

原 kind 的 lb-strict-0911、新集群 lb-validation、输入工作树与共享资源保留。未提交、推送、合并或发布。
